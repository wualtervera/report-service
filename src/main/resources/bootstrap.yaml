spring:
  application:
    name: report-service
  cloud:
    config:
      enabled: true
      uri:
        - http://localhost:8008
  data:
    mongodb:
      uri: mongodb://localhost:27017/bootcamp

#server.port=8007